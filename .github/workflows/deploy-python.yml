name: Deploy Python (Coolify)

on:
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Coolify deploy webhook
        run: curl -fsSL "${{ secrets.COOLIFY_PY_WEBHOOK }}"
